# 🏷️ 数据标注与数据集准备指南 (Task 2)

本指南将协助你完成 YOLOv8 自定义数据集的标注与组织工作。请严格按照以下步骤操作，以确保 `task2.py` 能够顺利运行。

## 1. 准备图片数据

*   **目标**：收集 50 张校园场景图片（如：校园内的行人、自行车、特定建筑物等）。
*   **格式**：推荐使用 `.jpg` 格式。确保图片清晰，光线适中。
*   **存放位置**：暂时将收集到的原始图片存放在 `data/custom_dataset/raw_images`（需新建此文件夹）中。

## 2. 数据标注 (Labeling)

推荐使用以下工具之一进行标注：

### 选项 A: LabelImg (本地工具，推荐初学者)
1.  **安装**：
    ```bash
    pip install labelImg
    labelImg
    ```
2.  **设置**：
    *   启动软件后，在左侧工具栏点击 **"Save format"** 按钮，切换为 **YOLO** 格式（确认为 `.txt` 文件）。
    *   设置 **Open Dir** 为你的图片文件夹。
    *   设置 **Change Save Dir** 为所有的标签 `.txt` 文件存放的文件夹。
3.  **开始标注**：
    *   按 `w` 创建选区框住目标。
    *   输入类别名称（如 `person`, `bicycle`）。
    *   按 `d` 切换下一张图片。
    *   **重要**：请务必记录下你的类别名称和对应的ID顺序（LabelImg classes.txt 文件中的顺序）。

### 选项 B: Roboflow (在线工具，功能强大)
1.  上传图片到 [Roboflow](https://roboflow.com/)。
2.  在线进行标注。
3.  导出数据集时选择 **YOLOv8** 格式。

## 3. 数据集组织结构 (Standard YOLO Format)

为了让 YOLOv8 自动读取，我们需要按照标准结构整理文件。

请手动将标注好的文件整理成如下结构：

```
my_yolo/
└── data/
    └── custom_dataset/
        ├── images/
        │   ├── train/      # 存放约 40 张训练集图片
        │   └── val/        # 存放约 10 张验证集图片
        └── labels/
            ├── train/      # 存放对应的 40 个训练集 txt 标签
            └── val/        # 存放对应的 10 个验证集 txt 标签
```

> **注意**：
> 1. 图片文件名和标签文件名必须完全一致（例如 `img_01.jpg` 对应 `img_01.txt`）。
> 2. `train` 和 `val` 文件夹内的图片数量比例建议为 8:2 或 7:3。

## 4. 创建数据集配置文件 (dataset.yaml)

在 `data/custom_dataset/` 目录下创建一个名为 `dataset.yaml` 的文件，内容如下：

```yaml
# dataset.yaml
path: ../data/custom_dataset  # 数据集根目录 (相对路径或绝对路径)
train: images/train           # 训练集图片路径 (相对于 path)
val: images/val               # 验证集图片路径 (相对于 path)

# 类别配置
nc: 2                         # 类别数量 (根据你的实际情况修改)
names: ['person', 'bike']     # 类别名称列表 (必须与标注时的顺序一致)
```

## 5. 验证准备工作

完成以上步骤后，你的目录看起来应该是这样的：

```
data/custom_dataset/
├── dataset.yaml
├── images
│   ├── train (40张图片)
│   └── val (10张图片)
└── labels
    ├── train (40个txt)
    └── val (10个txt)
```

完成后，即可运行 `task2.py` 开始训练！
